# Controlled semantic reconciliation — validation pending

Target: contract5.2.5/schema5.2, adapter0.2.2, temporal/source-time and narrow EVALv2
from contracts; exact final Isaac Sim6.1 / Kit110.3 and processorv3 from migrated master.
No gate is newly accepted by this merge resolution. S2 remains unresolved.

Historical registry ID collisions use explicit BASE/OTHER SHA suffixes. See
`historical-provenance.json` for original objects and exact ID/path mappings.
`history/` contains only committed files whose old hash would otherwise point to new
working content. Old evidence timestamps, commands and PASS/FAIL meanings are retained.
No historical pixels, environments or bulk runtime captures were copied.

Configured temporal limits are candidate constraints, not physical qualification.
Camera source-owned acquisition timing, atomic XR pose timing, physical limits and
complete Quest S2 evidence remain pending. EVAL endpoint identity binding to actual
runtime and core-vs-Isaac WebXR client guidance remain explicit validation follow-ups.

MANIFEST REGENERATION PENDING. The existing BASE MANIFEST bytes are retained only as
an unresolved generated-output task; they do not describe the merged tree. Run full
contract/core/runtime validation and then regenerate it. No merge commit or push yet.

Two OTHER visual-tuning manifests ended in a literal backslash-n after the JSON
object. Their readable JSON copies now have a real newline; original complete bytes
are retained under `history/0a0ed027/` with `.original` suffix. Object keys/hashes and
historical claims are unchanged. This is syntax normalization, not new evidence.
