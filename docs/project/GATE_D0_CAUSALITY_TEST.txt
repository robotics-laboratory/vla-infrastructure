Gate D0 causality and production temporal-recording regression
Timestamp: 2026-09-17T18:00:00+03:00
Command: uv run pytest -q tests/test_gate_d0_contract.py tests/test_d0_temporal_runtime.py tests/test_temporal_recording_path.py tests/test_lerobot_robot_piperx.py

................................                                         [100%]
32 passed in 6.66s

The executable frame seam remains the pinned LeRobot identity observation/action
processors plus build_dataset_frame, corresponding to lerobot_record.record_loop.
It proves pairs (obs_t=t, action_t=1000+t), explicitly rejects both t-1 and t+1
action shifts, and proves that a deliberately different send_action return is not the
dataset action source.

TemporalLeRobotDatasetAdapter is passed to the unmodified upstream record_loop and
delegates accepted frames to a real LeRobotDataset v3 writer. Logical timestamp remains
frame_index / dataset_fps; joint, left/right wrist-camera, XR-pose, and source-action
timing uses source-owned sequence/source_timestamp/clock_domain metadata. Persisted
left/right camera acquisition timestamps differ on the same logical dataset frame.

Stale camera, joint, and XR samples; future samples; missing timing metadata; excessive
cross-modal skew; incomparable clock domains; repeated/regressing sequences; and
source-timestamp regression all fail before DatasetWriter.add_frame. Episode reset
clears sequence validation state. Human-VR construction fails fast without a physical
XR acquisition-timing provider and never substitutes the existing host receipt time.

The PIPER-X regression proves the recording-only side-channel for bimanual joint state
and both wrist cameras, explicit SDK wall-clock to host-monotonic conversion, and
fail-closed behavior for missing SDK timestamps. The pre-existing action feature schema
is unchanged.
