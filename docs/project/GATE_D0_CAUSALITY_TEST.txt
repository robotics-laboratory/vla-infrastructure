Gate D0 focused causality and temporal-freshness regression
Timestamp: 2026-09-16T21:50:33+03:00
Command: uv run pytest -q tests/test_gate_d0_contract.py

.....                                                                    [100%]
5 passed in 1.99s

The executable frame seam remains the pinned LeRobot identity observation/action
processors plus build_dataset_frame, corresponding to lerobot_record.record_loop.
It proves pairs (obs_t=t, action_t=1000+t), explicitly rejects both t-1 and t+1
action shifts, and proves that a deliberately different send_action return is not the
dataset action source.

The D0 v2 temporal oracle separately proves that LeRobot logical timestamp
(frame_index / dataset_fps) is ineligible for freshness decisions; source age uses
source_timestamp in a common monotonic clock domain. Stale camera input, excessive
cross-modal skew, and incomparable clock domains fail closed. A repeated/slower source
sequence is not rejected by nominal rate alone while its age and skew remain admissible.

The optional LeRobot `datasets` extra is not installed in the accepted core profile,
so the policy-authorized narrow frame-construction fallback was used without changing
dependencies.
